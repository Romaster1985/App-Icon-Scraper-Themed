<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="32dp"
    android:gravity="center"
    android:background="?attr/android:windowBackground">

    <!-- Título principal -->
    <com.google.android.material.textview.MaterialTextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/preprocess_foreground"
        android:textAppearance="?attr/textAppearanceTitleLarge"
        android:gravity="center"
        android:layout_marginBottom="32dp"
        android:textColor="?attr/colorOnSurface" />

    <!-- Texto de estado (lo que antes era statusText) -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/starting_processing"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        android:textColor="?attr/colorOnSurfaceVariant" />

    <!-- Barra de progreso -->
    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progressBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:indicatorColor="?attr/colorPrimary"
        app:trackColor="?attr/colorSurfaceContainerHighest"
        android:layout_marginBottom="16dp"
        android:indeterminate="false" />

    <!-- Texto del progreso (ej: "5/50") -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/progressText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="0/0"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:gravity="center"
        android:layout_marginBottom="32dp"
        android:textColor="?attr/colorPrimary" />

    <!-- Botón de continuar (deshabilitado inicialmente) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/continueButton"
        style="?attr/materialButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/continue_to_customization"
        android:layout_marginTop="16dp"
        android:enabled="false" />

</LinearLayout>